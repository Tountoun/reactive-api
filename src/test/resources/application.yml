spring:
  application:
    name: spring-reactive application
  r2dbc:
    username: sa
    password:
    name: test_flex_bk_db
    url: r2dbc:h2:mem:///${spring.r2dbc.name};NON_KEYWORDS=transaction
  profiles:
    active: dev
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yml
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8085

beans:
  runner: false